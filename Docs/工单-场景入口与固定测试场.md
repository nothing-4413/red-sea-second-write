# 工单：场景入口与固定测试场

| 字段 | 内容 |
| --- | --- |
| 任务名称 | Bootstrap/Game/Test 三场景入口与固定案例运行器 |
| 所属模块 | Presentation / Testing / ProjectSettings |
| 输入数据 | Unity 场景入口模式、固定 seed 71、固定交换 `(1,3) → (1,4)` |
| 实现内容 | 增加 `SceneEntryPoint` 区分 Demo 与 FixedTests；TestScene 启动 `FixedTestRunner`，复用生产输入、Resolver、事件队列并输出 `[FixedTest] PASS/FAIL` |
| 涉及文件 | `SceneEntryPoint.cs`、`FixedTestRunner.cs`、`FixedTestScenario.cs`、`DemoBootstrap.cs`、三场景 YAML、ArchitectureTests 与文档 |
| 验收标准 | 三场景均进入 Build Settings；Bootstrap/Game 进入 Demo；Test 进入固定测试；固定案例覆盖有效交换、事件清空、稳定棋盘和终态点击 20 次 |
| 测试场景 | 独立 ArchitectureTests 调用固定场景逻辑；静态核对场景 GUID、入口模式与 Build Settings |
| 人工校验 | 在 Unity 2022.3 LTS 分别运行三场景，确认 Game 画面、Test Console 输出和 Bootstrap 装配无重复对象 |
| 已知风险 | 当前没有 Unity 编辑器，场景 YAML、生命周期和真实 Console 输出尚未做 PlayMode 验收 |

TestScene 不创建替代规则实现；`FixedTestScenario` 只作为生产模块的离线可执行入口。
